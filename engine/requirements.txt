# Core (required)
# No extra packages needed for the base engine

# Automation (for Screenshot / Mouse / Keyboard nodes)
mss>=9.0.1
pyautogui>=0.9.54
Pillow>=10.0.0

# AI providers (install as needed)
openai>=1.0.0
anthropic>=0.25.0
