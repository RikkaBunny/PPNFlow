# Core (required)
Pillow>=10.0.0
websockets>=12.0

# Screen capture & automation
mss>=9.0.1
pyautogui>=0.9.54
pygetwindow>=0.0.9

# OCR (default engine, auto-installed on first use)
# rapidocr>=3.0.0

# AI providers
openai>=1.0.0
anthropic>=0.25.0

# Optional OCR engines (install if you need them):
#   pip install rapidocr         ← recommended, ~30MB, no binary
#   pip install pytesseract      ← + Tesseract binary from https://github.com/UB-Mannheim/tesseract/wiki
#   pip install easyocr           ← ~200MB + PyTorch, 80+ languages
#   pip install winocr            ← Windows 10+ only, free, built-in

# Optional image processing:
#   pip install opencv-python     ← for Image Match (template matching)
